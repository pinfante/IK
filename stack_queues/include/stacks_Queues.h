#include <string>
#include <vector>

namespace stacks_Queues
{
	//HW PROBLEM #1
	int find_max_length_of_matching_parentheses(const std::string& brackets);

	//HW PROBLEM #2
	std::vector<int> max_in_sliding_window(const std::vector<int>& arr, size_t w);
};
